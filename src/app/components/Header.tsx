import { Flex } from "@/app/components/layout/Flex";

export const Header = () => {
    return (
        <Flex className="h-screen flex items-center justify-center">
            AAA
        </Flex>
    )
}